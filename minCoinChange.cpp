#include<iostream>
#include<vector>
using namespace std;
bool howSum(vector<int> array, int target)
{
if(traget ==0)
  return true;
if(target <-1)
  return false;
}


vector<int> howSum(vector<int> array, int target)
{
for(i:array)
{
 if(howSum(array,target-i));
}
retrun {};
}
